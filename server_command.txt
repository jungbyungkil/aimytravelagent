한국 기준으로 서버 시간 설정
sudo ln -sf /usr/share/zoneinfo/Asia/Seoul /etc/localtime

패키지 목록 업데이트 
sudo apt update

패키지 목록 업그레이드
sudo apt upgrade

pip3설치
sudo apt install python3-pip

가상환경 
sudo apt install python3.12-venv

가상환경 만들기
python3 -m venv bitcoinenv
source bitcoinenv/bin/activate

python3 -m venv stockcommentenv
source stockcommentenv/bin/activate

서버에서 라이브러리 설치
pip3 install -r requirements.txt

.env 파일 만들고 API KEY 넣기
vim .env

백그라운드 실행
nohup python3 -u autotrade.py > output.log 2>&1 &


nohup python3 -u stock_advisor.py > output.log 2>&1 &

프로세스 확인 
ps ax | grep .py

프로세스 중지
kill -9 프로세스번호

스트림릿 백그라운드 실행 
nohup python3 -m streamlit run streamlit_app.py --server.port 8501 > streamlit.log 2>&1 &

nohup python3 -m streamlit run stock_dashboard.py --server.port 8501 > streamlit.log 2>&1 &